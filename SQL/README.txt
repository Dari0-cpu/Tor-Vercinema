All'interno di questa cartella andranno (suddivisi) tutti i file sql
Lo schema a cui ho pensato presenta i seguenti file:

01_Schema.sql --> (DDL): Contiene i comandi CREATE TABLE e la definizione dei vincoli.

02_popolamento.sql --> (DML): Contiene i comandi INSERT INTO per riempire le tabelle.

03_query.sql --> (DML): Contiene le interrogazioni richieste (SELECT).

04_viste_e_indici.sql --> Contiene CREATE VIEW e CREATE INDEX per l'ottimizzazione fisica.